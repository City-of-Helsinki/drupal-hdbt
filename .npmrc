engine-strict=true
min-release-age=1
# The npm scripts run inside a alpine container, see tools/sandbox.sh.
# Force npm to install Linux/musl binaries regardless of which operating
# system the developer uses.
os=linux
libc=musl
