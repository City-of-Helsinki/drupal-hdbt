on: [push]
name: CI
jobs:
  tests:
    - uses: actions/checkout@v2
    - uses: setup-node@v1
        with:
          node-version: $(cat .nvmrc)

    - name: Install dependencies
      run: |
        npm i
        npx husky i
