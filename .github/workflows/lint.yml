on: [push]
name: Lint
jobs:
  lint:

    steps:
      - uses: actions/checkout@v2
      - uses: setup-node@v1
        with:
          node-version: $(cat .nvmrc)

      - name: Install dependencies
        run: |
          npm i
          npx husky i
