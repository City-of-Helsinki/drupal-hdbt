on:
  pull_request:
  push:
    branches:
      - main
name: CI
jobs:
  tests:
    strategy:
      matrix:
        php-versions: ['8.4']
    uses: city-of-helsinki/drupal-gh-actions/.github/workflows/theme-tests.yml@UHF-12748
    with:
      php_version: ${{ matrix.php-versions }}
    secrets:
      sonarcloud_token: ${{ secrets.SONAR_TOKEN }}
