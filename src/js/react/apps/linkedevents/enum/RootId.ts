const ROOT_ID = 'helfi-events-search';

// The id is kept for subthemes that still emit it instead of the class.
export const EVENTS_ROOT_SELECTOR = `.${ROOT_ID}, #${ROOT_ID}`;

export default ROOT_ID;
