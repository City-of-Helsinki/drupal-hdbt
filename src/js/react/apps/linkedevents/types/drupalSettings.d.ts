declare namespace drupalSettings {
  const path: {
    currentLanguage: 'fi' | 'en' | 'sv';
  };
  const helfi_events: {
    baseUrl: string,
    eventsUrl: string,
    imagePlaceholder: string
  };
}
