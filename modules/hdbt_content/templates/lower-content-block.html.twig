{#
/**
 * @file
 * Template for the lower content block.
 *
 * Variables:
 * - paragraphs: An array of paragraph entities
 * - computed: Automatically computed data from parent entity
 *
 * @see \Drupal\hdbt_content\Plugin\Block\LowerContentBlock
 */
#}

{% if paragraphs or computed %}
  <div class="lower-content layout-content components components--lower">
    {{ computed }}

    {% for paragraph in paragraphs %}
      {% if is_revision %}
        {{ drupal_entity_revision('paragraph', paragraph.target_revision_id) }}
      {% else %}
        {{ drupal_entity('paragraph', paragraph.target_id) }}
      {% endif %}
    {% endfor %}
  </div>
{% endif %}
