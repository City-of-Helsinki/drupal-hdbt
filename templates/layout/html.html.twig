{#
/**
 * @file
 * Theme override for the basic structure of a single Drupal page.
 *
 * Variables:
 * - logged_in: A flag indicating if user is logged in.
 * - root_path: The root path of the current page (e.g., node, admin, user).
 * - node_type: The content type for the current node, if the page is a node.
 * - head_title: List of text elements that make up the head_title variable.
 *   May contain one or more of the following:
 *   - title: The title of the page.
 *   - name: The name of the site.
 *   - slogan: The slogan of the site.
 * - page_top: Initial rendered markup. This should be printed before 'page'.
 * - page: The rendered page markup.
 * - page_bottom: Closing rendered markup. This variable should be printed after
 *   'page'.
 * - db_offline: A flag indicating if the database is offline.
 * - placeholder_token: The token for generating head, css, js and js-bottom
 *   placeholders.
 *
 * @see template_preprocess_html()
 */
#}
{% if not theme_color %}
  {% set theme_color = drupal_config('hdbt_admin_tools.site_settings', 'site_settings.theme_color')|default('bus') %}
{% endif %}

{# Available color themes, hardcoded to enable searching and to force thinking when adding these :) #}
{% if theme_color == 'bus'  %}
  {% set class = 'hdbt-theme--bus' %}
{% elseif theme_color == 'coat-of-arms' %}
  {% set class = 'hdbt-theme--coat-of-arms' %}
{% elseif theme_color == 'copper' %}
  {% set class = 'hdbt-theme--copper' %}
{% elseif theme_color == 'engel' %}
  {% set class = 'hdbt-theme--engel' %}
{% elseif theme_color == 'gold' %}
  {% set class = 'hdbt-theme--gold' %}
{% elseif theme_color == 'metro' %}
  {% set class = 'hdbt-theme--metro' %}
{% elseif theme_color == 'silver' %}
  {% set class = 'hdbt-theme--silver' %}
{% elseif theme_color == 'summer' %}
  {% set class = 'hdbt-theme--summer' %}
{% elseif theme_color == 'suomenlinna' %}
  {% set class = 'hdbt-theme--suomenlinna' %}
{% elseif theme_color == 'tram' %}
  {% set class = 'hdbt-theme--tram' %}
{% elseif theme_color == 'fog' %}
  {% set class = 'hdbt-theme--fog' %}
{% endif %}


<!DOCTYPE html>
<html{{ html_attributes.addClass(class) }}>
  <head>
    <head-placeholder token="{{ placeholder_token }}">
    <title>{{ head_title|safe_join(' | ') }}</title>
    <noscript><style>div.nav-toggle { display: none; }</style></noscript>
    <link rel="icon" href="{{ theme_path }}/src/images/favicon/favicon-32x32.ico" sizes="any">
    <link rel="icon" href="{{ theme_path }}/src/images/favicon/favicon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="{{ theme_path }}/src/images/favicon/apple-touch-icon.png">
    <link rel="manifest" href="{{ theme_path }}/src/images/favicon/manifest.webmanifest">
    <css-placeholder token="{{ placeholder_token }}">
    <js-placeholder token="{{ placeholder_token }}">
  </head>
  <body{{ attributes }}>
    {#
      Keyboard navigation/accessibility link to main content section in
      page.html.twig.
    #}
    <a href="#main-content" class="focusable skip-link skip-link--skip-to-main" id="skip-to-main">
      {{ 'Skip to main content'|t }}
    </a>
    {{ page_top }}
    {{ page }}
    {{ page_bottom }}
    <js-bottom-placeholder token="{{ placeholder_token }}">
  </body>
</html>
