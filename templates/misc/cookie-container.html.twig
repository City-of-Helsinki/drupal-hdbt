<script>
  window.hdsCookieConsentPageSettings = {
    language: '{{current_langcode}}', // Lang code defaults to 'en'
    //targetSelector: 'body', // Defaults to 'body'
    jsonUrl: '{{ theme_path }}/src/js/hds-cc/helfi_cookiesettings.json', // Path to site specific json settings
    exposeChatFunctions: true, // Defaults to false
    tempCssPath: '{{ theme_path }}/dist/css/cookie-consent.min.css', // TODO: Remove this when the real build process can include css files
  };
</script>
<script src="{{ theme_path }}/dist/js/hds-cc.min.js"></script>
