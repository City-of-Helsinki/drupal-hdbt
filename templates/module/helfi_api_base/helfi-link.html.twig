{% spaceless %}
  {{ title }}
  {% if 'data-is-external' in attributes|keys %}
    <span class="explain-external-icon"> ({{ 'Link leads to external service'|t({}, {'context': 'Explanation for screen-reader software that the icon visible next to this link means that the link leads to an external service.'}) }})</span>
  {% endif %}
{% endspaceless %}
