{# simplified from paragraph.html.twig to avoid wrapping library items in extra divs #}
{% if paragraph.isNotPublished() %}
 <div class="component from-library--unpublished">
  {{ content }}
  </div>
{% else %}
  {{ content }}
{% endif %}
